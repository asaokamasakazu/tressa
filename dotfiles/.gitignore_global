.DS_Store

# Build output directory
dist/

# Claude Code
.claude/CLAUDE.md
.claude/local/*
.claude/commands/local/*

# Damrey: `yarn set version classic` で生成されたファイル（damreyでpre-commit時のyarnのバージョン不一致を回避するために実行したら作成された）
.yarnrc.yml
.yarn/releases/yarn-classic.cjs

# Damrey: RubyMineが自動で生成するファイル
engines/ad_manager_client/Gemfile.lock
engines/admin/bbs_admin/Gemfile.lock
